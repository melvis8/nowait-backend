# notifications_utils.py
async def send_notification_async(user_id: int, type: str, message: str):
    # Your actual logic to send notifications
    print(f"Sending {type} notification to user {user_id}: {message}")
